version: "3.8"

services:
  api:
    build: .
    ports:
      - "3000:3000" # يربط المنفذ 3000 بالمنفذ 3000 داخل الحاوية
    environment:
      - MONGODB_URL=mongodb://mongo:27017/ali
      - CLOUD_NAME=xkectvmn
      - API_KEY_CLOUD=547842524227624
    volumes:
      - ./uploads:/app/uploads # لمزامنة الصور المرفوعة محلياً
    depends_on:
      - mongo # يضمن عدم تشغيل التطبيق إلا بعد إقلاع قاعدة البيانات أولاً

  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
